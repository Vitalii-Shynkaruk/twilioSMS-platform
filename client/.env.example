# Client Environment Variables

# Только локальный QA. Показывает кнопку dev-mode login в Vite dev builds.
# Production builds скрывают эту кнопку даже при случайно включенном flag.
VITE_DEV_MODE_LOGIN_ENABLED=false