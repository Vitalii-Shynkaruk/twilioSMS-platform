# Client Environment Variables

# Local QA only. Shows the dev-mode login button in Vite development builds.
# Production builds hide this button even if the flag is enabled by mistake.
VITE_DEV_MODE_LOGIN_ENABLED=false